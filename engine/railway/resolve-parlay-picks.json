{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "dockerfile",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "python -m system.resolve_parlay_picks",
    "numReplicas": 1,
    "sleepApplication": false,
    "restartPolicyType": "always"
  }
}