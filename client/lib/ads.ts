import { TestIds } from "react-native-google-mobile-ads";

// TODO add change to use actual unit
export const adaptiveBannerUnitId = __DEV__ ? TestIds.ADAPTIVE_BANNER : "";
export const interstitialAdUnitId =  __DEV__ ? TestIds.INTERSTITIAL : ""